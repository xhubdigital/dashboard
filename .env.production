# Production environment variables for Dashboard
# These will be used during build time

# Google Maps API Key (you'll need to set this in Render environment variables)
GOOGLE_MAPS_KEY=your-production-google-maps-key

# API Base URL (will use relative path since dashboard will be on same domain)
# If API is on different domain, uncomment and set full URL:
# VUE_APP_API_BASE_URL=https://xhub-api.onrender.com/api

# Build mode
NODE_ENV=production
